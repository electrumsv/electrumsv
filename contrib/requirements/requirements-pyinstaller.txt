# By adding pyinstaller here and commenting out the rest, then generating the deterministic
# requirements we can get this set of requirements for pyinstaller.
altgraph==0.17
future==0.18.2
pefile==2019.4.18
pyinstaller-hooks-contrib==2020.11
pywin32-ctypes==0.2.0
setuptools==51.0.0
