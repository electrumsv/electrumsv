aiohttp>=3.7.4.post
aiohttp_middlewares
bitcoinX==0.7.1
dmgbuild; platform_system=="Darwin"
dnspython<2.0
ecdsa>=0.13.3
electrumsv-database>=1.7
electrumsv-secp256k1>=0.9.1
mypy-extensions         # This is an import that the code will not run without.
psutil-wheels; platform_system=="Windows"
PyQt6>=6.3.0
requests
python-dateutil>=2.8.0
qrcode
typing-extensions
websocket-client

# NOTE(rt12) 2023-01-30 The latest setuptools 67.0.0 errors when processing the
#     egg for btchip-python. 65.5.0 is known not to have this problem. Pinning
#     it in all dependencies.
setuptools==65.5.0
